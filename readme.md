Cara Run Program:

Buat virtual environment (jika belum ada):
python -m venv venv

Aktivasi venv:
venv\Scripts\activate

Install dependencies:
pip install -r requirements.txt

Training model, jalankan:
python face_train.py

Jalankan program utama (GUI absensi wajah):
python face_recognition.gui.py
